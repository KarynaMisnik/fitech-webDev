/* Returning first value in an array */
const peek = (array) => {
  return array[0];
  // implement here
};

export default peek;

/* Removing first and last item from an array */
const squeeze = (array) => {
    array.shift()
    array.pop()
  // implement here
};

export default squeeze;